export function greetEvening(name: string): string {
  return `Good evening, ${name}!`;
}
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function greetMorning(name: string): string {
  return `Good morning, ${name}!`;
}
